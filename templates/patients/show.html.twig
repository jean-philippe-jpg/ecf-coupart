{% extends 'base.html.twig' %}

{% block title %}Patients{% endblock %}

{% block body %}
    <h1>Patients</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ patient.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ patient.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ patient.prenom }}</td>
            </tr>
            <tr>
                <th>Allergenes</th>
                <td>{{ patient.allergenes }}</td>
            </tr>
            <tr>
                <th>Regime</th>
                <td>{{ patient.regime }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_patients_index') }}">back to list</a>

    <a href="{{ path('app_patients_edit', {'id': patient.id}) }}">edit</a>

    {{ include('patients/_delete_form.html.twig') }}
{% endblock %}
